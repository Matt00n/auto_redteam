I have a very interesting and challenging machine learning research problem which is at the intersection of security (specifically red teaming) and multi-agent systems (LLM-based). specifically want to develop a system which will iteratively propose attack vectors and ideally also executes them and constantly learns and evolves from past attempts. at the same time we need to be creative to also come up with new (and even farfetched) ideas. the system may also additionally propose attack vectors requiring human involvement (e.g. for execution). the final output of the system should be all the attack vectors (prominently those which work, but we want to log - ideally systematically - all attempted as well). our specific instance we focus on as a website we created ourselves. the website contains an editor which does not allow pastes. the goal of the attacks should be to bypass this defense (specifically getting text into the text box without the human actually typing). we can give the full frontend code to the system as context and potentially also the defense-related backend code (we might also want to have different agents, some with full, some with partial infos). for the automated attacks part, the system will interact with a locally deployed version (though again non-automated attacks are also allowed). a successfull attack will be defined by getting an arbitrarily long text (only few characters is insufficient) into the text box within a reasonable time frame AND that the text stays in the text box upon reloading the page. We need to make sure that we measure this correctly (either empty the text box before each attempt or diff to status quo)